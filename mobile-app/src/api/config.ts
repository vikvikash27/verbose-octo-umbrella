// IMPORTANT: Replace 'YOUR_COMPUTER_IP' with the local IP address of the machine
// running the backend server.
// On macOS/Linux, you can find this by running `ifconfig` in the terminal.
// On Windows, run `ipconfig`.
// Your phone and computer must be on the same Wi-Fi network.
// Example: 'http://192.168.1.10:3001'

export const API_URL = 'http://YOUR_COMPUTER_IP:3001';
